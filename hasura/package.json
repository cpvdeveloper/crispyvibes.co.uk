{
  "name": "hasura",
  "version": "1.0.0",
  "scripts": {
    "hasura:console": "hasura console --admin-secret ${npm_config_secret}",
    "hasura:migrate:apply": "hasura migrate apply --admin-secret ${npm_config_secret}",
    "hasura:meta:apply": "hasura metadata apply --admin-secret ${npm_config_secret}",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}